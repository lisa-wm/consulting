# ------------------------------------------------------------------------------
# CREATION OF UNIQUE DOC IDS
# ------------------------------------------------------------------------------

# PURPOSE: create unique doc IDs that are concatenated from user name, timestamp 
# and a unique encoding of the text, so uniqueness will be preserved even when 
# new data are added

make_doc_id <- function(doc) {
  
  
  
}
